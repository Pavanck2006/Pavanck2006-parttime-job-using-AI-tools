require('dotenv').config();
const express = require('express');
const path = require('path');
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');
const {pool, initializeDatabase, transaction} = require('./server/db');
const chatRouter = require('./server/chat');
const app = express();
const PORT = Number(process.env.PORT || 8080);
const SECRET = process.env.JWT_SECRET || 'development-secret-change-me';
const staticDir = path.join(__dirname, 'src', 'main', 'resources', 'static');
app.use(express.json({limit: '1mb'})); app.use(express.urlencoded({extended: true}));
const ok = (res, data, message) => res.json({data, ...(message ? {message} : {})});
const fail = (res, status, message) => res.status(status).json({message});
const guard = (...roles) => (req, res, next) => req.user && roles.includes(req.user.role) ? next() : fail(res, 403, 'You do not have permission for this action');
app.use('/api/reports/:id', (req, res, next) => req.method === 'DELETE' ? auth(req, res, () => guard('ROLE_STUDENT')(req, res, next)) : next());
app.use('/api/chat', auth, chatRouter);
async function auth(req, res, next) { try { const token = (req.headers.authorization || '').replace(/^Bearer\s+/i, ''); if (!token) return fail(res, 401, 'Authentication is required'); const c = jwt.verify(token, SECRET); const [r] = await pool.query('SELECT * FROM users WHERE id=?', [c.id]); if (!r[0] || !r[0].is_active || r[0].is_suspended) return fail(res, 401, 'Account is inactive or suspended'); req.user = r[0]; next(); } catch { fail(res, 401, 'Invalid or expired token'); } }
function token(user, extra = {}) { return {token: jwt.sign({id: user.id, role: user.role}, SECRET, {expiresIn: '7d'}), type: 'Bearer', id: user.id, email: user.email, fullName: user.full_name, phone: user.phone, role: user.role, active: !!user.is_active, suspended: !!user.is_suspended, ...extra}; }
const jobSql = `SELECT j.*,o.catering_name,o.verification_status,u.id owner_user_id,u.full_name owner_name FROM catering_jobs j JOIN owner_profiles o ON o.id=j.owner_id JOIN users u ON u.id=o.user_id`;
const appSql = `SELECT a.*,j.title job_title,j.work_type,j.work_area,j.detailed_location,j.job_date,j.start_time,j.end_time,j.payment_type,j.is_on_spot_payment,j.contact_phone,j.contact_email,o.catering_name,o.verification_status,ou.id owner_user_id,ou.full_name owner_name,su.id student_user_id,su.full_name student_name,su.email student_email,su.phone student_phone,sp.college_name,sp.skills,sp.rating student_rating,sp.total_jobs_completed FROM job_applications a JOIN catering_jobs j ON j.id=a.job_id JOIN owner_profiles o ON o.id=j.owner_id JOIN users ou ON ou.id=o.user_id JOIN student_profiles sp ON sp.id=a.student_id JOIN users su ON su.id=sp.user_id`;
function job(r, unlocked = false) { return {id:r.id,title:r.title,description:r.description,workType:r.work_type,workTypeDisplayName:r.work_type,workArea:r.work_area,detailedLocation:unlocked?r.detailed_location:undefined,jobDate:r.job_date,startTime:r.start_time,endTime:r.end_time,paymentAmount:r.payment_amount,paymentType:r.payment_type,paymentTypeDisplayName:r.payment_type,onSpotPayment:!!r.is_on_spot_payment,workersRequired:r.workers_required,workersSelected:r.workers_selected,requiredSkills:r.required_skills,contactPhone:unlocked?r.contact_phone:undefined,contactEmail:unlocked?r.contact_email:undefined,status:r.status,cateringName:r.catering_name,ownerId:r.owner_user_id,ownerVerified:r.verification_status==='VERIFIED',createdAt:r.created_at}; }
function application(r, unlocked = false) { return {id:r.id,jobId:r.job_id,jobTitle:r.job_title,workType:r.work_type,workTypeDisplayName:r.work_type,workArea:r.work_area,detailedLocation:unlocked?r.detailed_location:undefined,locationUnlocked:unlocked,jobDate:r.job_date,startTime:r.start_time,endTime:r.end_time,paymentType:r.payment_type,paymentTypeDisplayName:r.payment_type,onSpotPayment:!!r.is_on_spot_payment,contactPhone:unlocked?r.contact_phone:undefined,contactEmail:unlocked?r.contact_email:undefined,contactUnlocked:unlocked,ownerId:r.owner_user_id,cateringName:r.catering_name,ownerName:r.owner_name,ownerVerified:r.verification_status==='VERIFIED',studentId:r.student_id,studentUserId:r.student_user_id,studentName:r.student_name,studentEmail:r.student_email,studentPhone:r.student_phone,collegeName:r.college_name,skills:r.skills,studentRating:r.student_rating,totalJobsCompleted:r.total_jobs_completed,status:r.status,attendanceStatus:r.attendance_status,workCompletionStatus:r.work_completion_status,paymentStatus:r.payment_status,paymentAmount:r.payment_amount,paymentConfirmationDate:r.payment_confirmation_date,notes:r.notes,appliedAt:r.applied_at,respondedAt:r.responded_at,createdAt:r.created_at}; }
function profile(r) { return r ? {id:r.id,userId:r.user_id,fullName:r.full_name,email:r.email,phone:r.phone,collegeName:r.college_name,preferredArea:r.preferred_area,skills:r.skills,bio:r.bio,emergencyContact:r.emergency_contact,totalJobsCompleted:r.total_jobs_completed,rating:r.rating,cateringName:r.catering_name,businessAddress:r.business_address,businessPhone:r.business_phone,verified:r.verification_status==='VERIFIED',verificationStatus:r.verification_status,verifiedAt:r.verified_at,totalJobsPosted:r.total_jobs_posted} : null; }
async function listJobs(req,res,next) { try { let sql=`${jobSql} WHERE j.status='OPEN' AND u.is_suspended=FALSE`, v=[]; const q=req.query; if(q.area||q.location){sql+=' AND LOWER(j.work_area) LIKE LOWER(?)';v.push(`%${q.area||q.location}%`);} if(q.workType){sql+=' AND j.work_type=?';v.push(q.workType);} if(q.jobDate){sql+=' AND j.job_date=?';v.push(q.jobDate);} if(q.minPayment){sql+=' AND j.payment_amount>=?';v.push(q.minPayment);} if(q.maxPayment){sql+=' AND j.payment_amount<=?';v.push(q.maxPayment);} if(q.paymentType){sql+=' AND j.payment_type=?';v.push(q.paymentType);} sql+=' ORDER BY j.job_date ASC,j.created_at DESC'; const [r]=await pool.query(sql,v); ok(res,r.map(x=>job(x)),'Jobs retrieved successfully'); } catch(e){next(e);} }
app.post('/api/auth/register',async(req,res,next)=>{try{const b=req.body;if(!b.fullName||!b.email||!b.password||!b.phone||!['ROLE_STUDENT','ROLE_OWNER'].includes(b.role))return fail(res,400,'Full name, email, password, phone and a valid role are required');const data=await transaction(async c=>{const [x]=await c.query('INSERT INTO users (email,password_hash,full_name,phone,role) VALUES (?,?,?,?,?)',[b.email.toLowerCase(),await bcrypt.hash(b.password,10),b.fullName,b.phone,b.role]);if(b.role==='ROLE_STUDENT')await c.query('INSERT INTO student_profiles (user_id,college_name,preferred_area,skills,bio,emergency_contact) VALUES (?,?,?,?,?,?)',[x.insertId,b.collegeName||null,b.preferredArea||null,b.skills||null,b.bio||null,b.emergencyContact||null]);else await c.query('INSERT INTO owner_profiles (user_id,catering_name,business_address,business_phone) VALUES (?,?,?,?)',[x.insertId,b.cateringName||b.fullName,b.businessAddress||null,b.businessPhone||b.phone]);const [u]=await c.query('SELECT * FROM users WHERE id=?',[x.insertId]);return token(u[0]);});ok(res,data,'Registration successful! Welcome to PartTime Job.');}catch(e){if(e.code==='ER_DUP_ENTRY')return fail(res,409,'Email is already registered');next(e);}});
app.post('/api/auth/login',async(req,res,next)=>{try{const [r]=await pool.query('SELECT * FROM users WHERE email=?',[(req.body.email||'').toLowerCase()]);const u=r[0];if(!u||!(await bcrypt.compare(req.body.password||'',u.password_hash)))return fail(res,401,'Invalid email or password');if(u.is_suspended||!u.is_active)return fail(res,403,'Your account is suspended or inactive');let extra={};if(u.role==='ROLE_STUDENT'){const [p]=await pool.query('SELECT preferred_area FROM student_profiles WHERE user_id=?',[u.id]);extra.preferredArea=p[0]?.preferred_area;}if(u.role==='ROLE_OWNER'){const [p]=await pool.query('SELECT id,catering_name,verification_status FROM owner_profiles WHERE user_id=?',[u.id]);extra={profileId:p[0]?.id,cateringName:p[0]?.catering_name,verificationStatus:p[0]?.verification_status};}ok(res,token(u,extra),'Login successful!');}catch(e){next(e);}});
app.get('/api/public/jobs',listJobs); app.get('/api/public/jobs/recommended',listJobs);
app.get('/api/public/jobs/:id',async(req,res,next)=>{try{const [r]=await pool.query(`${jobSql} WHERE j.id=?`,[req.params.id]);if(!r[0])return fail(res,404,'Job not found');ok(res,job(r[0]),'Job details retrieved');}catch(e){next(e);}});
async function current(req, kind){const table=kind==='student'?'student_profiles':'owner_profiles';const [r]=await pool.query(`SELECT u.*,p.* FROM users u JOIN ${table} p ON p.user_id=u.id WHERE u.id=?`,[req.user.id]);return r[0];}
app.get('/api/student/profile',auth,guard('ROLE_STUDENT'),async(req,res,next)=>{try{ok(res,profile(await current(req,'student')));}catch(e){next(e);}});
app.put('/api/student/profile',auth,guard('ROLE_STUDENT'),async(req,res,next)=>{try{const b=req.body;await transaction(async c=>{await c.query('UPDATE users SET full_name=COALESCE(?,full_name),phone=COALESCE(?,phone) WHERE id=?',[b.fullName,b.phone,req.user.id]);await c.query('UPDATE student_profiles SET college_name=?,preferred_area=?,skills=?,bio=?,emergency_contact=? WHERE user_id=?',[b.collegeName||null,b.preferredArea||null,b.skills||null,b.bio||null,b.emergencyContact||null,req.user.id]);});ok(res,profile(await current(req,'student')),'Profile updated successfully');}catch(e){next(e);}});
app.get('/api/student/dashboard',auth,guard('ROLE_STUDENT'),async(req,res,next)=>{try{const [r]=await pool.query(`SELECT (SELECT COUNT(*) FROM catering_jobs WHERE status='OPEN') availableJobsCount,(SELECT COUNT(*) FROM job_applications a JOIN student_profiles s ON s.id=a.student_id WHERE s.user_id=?) totalApplicationsCount,(SELECT COUNT(*) FROM job_applications a JOIN student_profiles s ON s.id=a.student_id WHERE s.user_id=? AND a.status='ACCEPTED') acceptedApplicationsCount,(SELECT COUNT(*) FROM job_applications a JOIN student_profiles s ON s.id=a.student_id WHERE s.user_id=? AND a.work_completion_status='COMPLETED') completedJobsCount,(SELECT COALESCE(SUM(payment_amount),0) FROM job_applications a JOIN student_profiles s ON s.id=a.student_id WHERE s.user_id=? AND a.payment_status='CONFIRMED') totalEarnings`,[req.user.id,req.user.id,req.user.id,req.user.id]);ok(res,r[0]);}catch(e){next(e);}}); app.get('/api/student/jobs',auth,guard('ROLE_STUDENT'),listJobs);
app.post('/api/student/jobs/:jobId/apply',auth,guard('ROLE_STUDENT'),async(req,res,next)=>{try{const data=await transaction(async c=>{const [[s]] = await c.query('SELECT id FROM student_profiles WHERE user_id=?',[req.user.id]);const [[j]]=await c.query('SELECT * FROM catering_jobs WHERE id=? FOR UPDATE',[req.params.jobId]);if(!s||!j||j.status!=='OPEN')throw Object.assign(new Error('Job is not available'),{status:400});const [x]=await c.query('INSERT INTO job_applications (job_id,student_id,payment_amount,notes) VALUES (?,?,?,?)',[j.id,s.id,j.payment_amount,req.body.notes||null]);const [r]=await c.query(`${appSql} WHERE a.id=?`,[x.insertId]);return application(r[0]);});ok(res,data,'Application submitted successfully!');}catch(e){if(e.code==='ER_DUP_ENTRY')return fail(res,409,'You have already applied for this job');next(e);}});
async function studentApps(req,res,next,filter=''){try{const [r]=await pool.query(`${appSql} WHERE su.id=? ${filter} ORDER BY a.created_at DESC`,[req.user.id]);ok(res,r.map(x=>application(x,x.status==='ACCEPTED'||x.work_completion_status==='COMPLETED')));}catch(e){next(e);}} app.get('/api/student/applications',auth,guard('ROLE_STUDENT'),studentApps);app.get('/api/student/applications/accepted',auth,guard('ROLE_STUDENT'),(q,s,n)=>studentApps(q,s,n,"AND a.status='ACCEPTED'") );app.get('/api/student/applications/completed',auth,guard('ROLE_STUDENT'),(q,s,n)=>studentApps(q,s,n,"AND a.work_completion_status='COMPLETED'") );
app.delete('/api/student/applications/:id',auth,guard('ROLE_STUDENT'),async(req,res,next)=>{try{const [r]=await pool.query("UPDATE job_applications a JOIN student_profiles s ON s.id=a.student_id SET a.status='CANCELLED' WHERE a.id=? AND s.user_id=? AND a.status='APPLIED'",[req.params.id,req.user.id]);if(!r.affectedRows)return fail(res,400,'Only pending applications can be cancelled');ok(res,null,'Application cancelled successfully');}catch(e){next(e);}});
app.put('/api/student/applications/:id/confirm-payment',auth,guard('ROLE_STUDENT'),async(req,res,next)=>{try{await transaction(async c=>{const [r]=await c.query("UPDATE payment_records p JOIN student_profiles s ON s.id=p.student_id SET p.payment_status='CONFIRMED',p.confirmed_paid_at=NOW(),p.notes=? WHERE p.application_id=? AND s.user_id=?",[req.body.notes||null,req.params.id,req.user.id]);if(!r.affectedRows)throw Object.assign(new Error('Payment record not found'),{status:404});await c.query("UPDATE job_applications SET payment_status='CONFIRMED',payment_confirmation_date=NOW() WHERE id=?",[req.params.id]);});ok(res,null,'Payment confirmed successfully!');}catch(e){next(e);}});app.get('/api/student/payments',auth,guard('ROLE_STUDENT'),async(req,res,next)=>{try{const [r]=await pool.query('SELECT p.*,j.title job_title,j.work_area FROM payment_records p JOIN catering_jobs j ON j.id=p.job_id JOIN student_profiles s ON s.id=p.student_id WHERE s.user_id=?',[req.user.id]);ok(res,r);}catch(e){next(e);}});
async function ownerId(req){const [r]=await pool.query('SELECT id FROM owner_profiles WHERE user_id=?',[req.user.id]);return r[0]?.id;}
app.get('/api/owner/profile',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{ok(res,profile(await current(req,'owner')));}catch(e){next(e);}});app.put('/api/owner/profile',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const b=req.body;await transaction(async c=>{await c.query('UPDATE users SET full_name=COALESCE(?,full_name),phone=COALESCE(?,phone) WHERE id=?',[b.fullName,b.phone,req.user.id]);await c.query('UPDATE owner_profiles SET catering_name=COALESCE(?,catering_name),business_address=?,business_phone=? WHERE user_id=?',[b.cateringName,b.businessAddress||null,b.businessPhone||null,req.user.id]);});ok(res,profile(await current(req,'owner')),'Profile updated successfully');}catch(e){next(e);}});
app.post('/api/owner/jobs',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const b=req.body,id=await ownerId(req);const [x]=await pool.query('INSERT INTO catering_jobs (owner_id,title,description,work_type,work_area,detailed_location,job_date,start_time,end_time,payment_amount,payment_type,is_on_spot_payment,workers_required,required_skills,contact_phone,contact_email) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)',[id,b.title,b.description||null,b.workType,b.workArea,b.detailedLocation,b.jobDate,b.startTime,b.endTime,b.paymentAmount,b.paymentType,b.onSpotPayment!==false,b.workersRequired,b.requiredSkills||null,b.contactPhone,b.contactEmail||null]);const [r]=await pool.query(`${jobSql} WHERE j.id=?`,[x.insertId]);ok(res,job(r[0],true),'Job posted successfully!');}catch(e){next(e);}});
app.get('/api/owner/jobs',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const [r]=await pool.query(`${jobSql} WHERE j.owner_id=? ORDER BY j.created_at DESC`,[await ownerId(req)]);ok(res,r.map(x=>job(x,true)));}catch(e){next(e);}});app.get('/api/owner/jobs/:id',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const [r]=await pool.query(`${jobSql} WHERE j.id=? AND j.owner_id=?`,[req.params.id,await ownerId(req)]);if(!r[0])return fail(res,404,'Job not found');const [a]=await pool.query(`${appSql} WHERE j.id=?`,[req.params.id]);ok(res,{...job(r[0],true),applications:a.map(x=>application(x,true))});}catch(e){next(e);}});
app.put('/api/owner/jobs/:id',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const b=req.body;await pool.query('UPDATE catering_jobs SET title=?,description=?,work_type=?,work_area=?,detailed_location=?,job_date=?,start_time=?,end_time=?,payment_amount=?,payment_type=?,is_on_spot_payment=?,workers_required=?,required_skills=?,contact_phone=?,contact_email=? WHERE id=? AND owner_id=?',[b.title,b.description,b.workType,b.workArea,b.detailedLocation,b.jobDate,b.startTime,b.endTime,b.paymentAmount,b.paymentType,b.onSpotPayment,b.workersRequired,b.requiredSkills,b.contactPhone,b.contactEmail,req.params.id,await ownerId(req)]);const [r]=await pool.query(`${jobSql} WHERE j.id=?`,[req.params.id]);ok(res,job(r[0],true),'Job updated successfully');}catch(e){next(e);}});app.delete('/api/owner/jobs/:id',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{await pool.query("UPDATE catering_jobs SET status='CANCELLED' WHERE id=? AND owner_id=?",[req.params.id,await ownerId(req)]);ok(res,null,'Job cancelled successfully');}catch(e){next(e);}});app.get('/api/owner/jobs/:id/applications',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const [r]=await pool.query(`${appSql} WHERE j.id=? AND j.owner_id=?`,[req.params.id,await ownerId(req)]);ok(res,r.map(x=>application(x,true)));}catch(e){next(e);}});
async function appAction(req,res,next,accept){try{const id=await ownerId(req);const data=await transaction(async c=>{const [r]=await c.query(`${appSql} WHERE a.id=? AND j.owner_id=? FOR UPDATE`,[req.params.id,id]);const a=r[0];if(!a)throw Object.assign(new Error('Application not found'),{status:404});if(accept){const [[count]]=await c.query("SELECT COUNT(*) count FROM job_applications WHERE job_id=? AND status='ACCEPTED'",[a.job_id]);const [[j]]=await c.query('SELECT workers_required FROM catering_jobs WHERE id=?',[a.job_id]);if(count.count>=j.workers_required)throw Object.assign(new Error('Worker limit has been reached'),{status:400});await c.query("UPDATE job_applications SET status='ACCEPTED',responded_at=NOW() WHERE id=?",[a.id]);await c.query("UPDATE catering_jobs SET workers_selected=workers_selected+1,status=IF(workers_selected+1>=workers_required,'FILLED','OPEN') WHERE id=?",[a.job_id]);await c.query('INSERT INTO payment_records (application_id,job_id,student_id,owner_id,amount,payment_type) SELECT a.id,a.job_id,a.student_id,?,a.payment_amount,j.payment_type FROM job_applications a JOIN catering_jobs j ON j.id=a.job_id WHERE a.id=?',[id,a.id]);await c.query("INSERT INTO notifications (recipient_id,title,message,type,related_entity_id) VALUES (?,?,?,?,?)",[a.student_user_id,'Application Accepted',`Your application for \"${a.job_title}\" was accepted. Check Accepted Shifts for venue and contact details.`,'APPLICATION_ACCEPTED',a.id]);}else{await c.query("UPDATE job_applications SET status='REJECTED',responded_at=NOW() WHERE id=?",[a.id]);await c.query("INSERT INTO notifications (recipient_id,title,message,type,related_entity_id) VALUES (?,?,?,?,?)",[a.student_user_id,'Application Update',`Your application for \"${a.job_title}\" was not accepted.`,'APPLICATION_REJECTED',a.id]);}const [x]=await c.query(`${appSql} WHERE a.id=?`,[a.id]);return application(x[0],true);});ok(res,data,accept?'Applicant accepted successfully!':'Applicant rejected.');}catch(e){next(e);}}app.put('/api/owner/applications/:id/accept',auth,guard('ROLE_OWNER'),(q,s,n)=>appAction(q,s,n,true));app.put('/api/owner/applications/:id/reject',auth,guard('ROLE_OWNER'),(q,s,n)=>appAction(q,s,n,false));
app.put('/api/owner/applications/:id/attendance',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{await pool.query('UPDATE job_applications a JOIN catering_jobs j ON j.id=a.job_id SET a.attendance_status=?,a.work_completion_status=? WHERE a.id=? AND j.owner_id=?',[req.body.attendanceStatus,req.body.workCompletionStatus||'COMPLETED',req.params.id,await ownerId(req)]);ok(res,null,'Attendance updated successfully');}catch(e){next(e);}});app.put('/api/owner/jobs/:id/complete',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const id=await ownerId(req);await transaction(async c=>{await c.query("UPDATE catering_jobs SET status='COMPLETED' WHERE id=? AND owner_id=?",[req.params.id,id]);await c.query("UPDATE job_applications SET work_completion_status='COMPLETED' WHERE job_id=? AND status='ACCEPTED'",[req.params.id]);});ok(res,null,'Job marked as completed!');}catch(e){next(e);}});app.put('/api/owner/applications/:id/payment',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{await transaction(async c=>{await c.query("UPDATE payment_records SET payment_status='PAID',marked_paid_at=NOW(),notes=? WHERE application_id=? AND owner_id=?",[req.body.notes||null,req.params.id,await ownerId(req)]);await c.query("UPDATE job_applications SET payment_status='PAID' WHERE id=?",[req.params.id]);});ok(res,null,'Payment marked as PAID successfully!');}catch(e){next(e);}});app.get('/api/owner/payments',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const [r]=await pool.query('SELECT p.*,j.title job_title,j.work_area,su.full_name student_name,su.email student_email FROM payment_records p JOIN catering_jobs j ON j.id=p.job_id JOIN student_profiles s ON s.id=p.student_id JOIN users su ON su.id=s.user_id WHERE p.owner_id=?',[await ownerId(req)]);ok(res,r);}catch(e){next(e);}});
app.get('/api/owner/complaints',auth,guard('ROLE_OWNER'),async(req,res,next)=>{try{const [r]=await pool.query("SELECT r.*,j.title job_title,j.work_area,j.job_date,su.full_name student_name,su.email student_email,su.phone student_phone,sp.skills student_skills,sp.preferred_area student_area,a.status application_status,a.payment_status application_payment_status,a.payment_amount assigned_amount FROM reports r JOIN users su ON su.id=r.reporter_id LEFT JOIN student_profiles sp ON sp.user_id=su.id LEFT JOIN catering_jobs j ON j.id=r.job_id LEFT JOIN job_applications a ON a.id=r.application_id WHERE r.target_user_id=? ORDER BY r.created_at DESC",[req.user.id]);ok(res,r.map(x=>({...x,studentName:x.student_name,studentEmail:x.student_email,studentPhone:x.student_phone,studentSkills:x.student_skills,studentArea:x.student_area,jobTitle:x.job_title,workArea:x.work_area,jobDate:x.job_date,applicationStatus:x.application_status,applicationPaymentStatus:x.application_payment_status,assignedAmount:x.assigned_amount,reportType:x.report_type,expectedAmount:x.expected_amount,receivedAmount:x.received_amount,evidenceNotes:x.evidence_notes,adminRemarks:x.admin_remarks,createdAt:x.created_at})));}catch(e){next(e);}});
app.get('/api/admin/dashboard',auth,guard('ROLE_ADMIN'),async(req,res,next)=>{try{const [r]=await pool.query(`SELECT (SELECT COUNT(*) FROM student_profiles) totalStudentsCount,(SELECT COUNT(*) FROM owner_profiles) totalOwnersCount,(SELECT COUNT(*) FROM owner_profiles WHERE verification_status='VERIFIED') verifiedOwnersCount,(SELECT COUNT(*) FROM catering_jobs WHERE status IN ('OPEN','FILLED')) totalActiveJobsCount,(SELECT COUNT(*) FROM catering_jobs WHERE status='COMPLETED') totalCompletedJobsCount,(SELECT COUNT(*) FROM job_applications) totalApplicationsCount,(SELECT COUNT(*) FROM reports WHERE status='PENDING') pendingDisputesCount,(SELECT COUNT(*) FROM users WHERE is_suspended=TRUE) suspendedUsersCount,(SELECT COALESCE(SUM(amount),0) FROM payment_records WHERE payment_status IN ('PAID','CONFIRMED')) totalPlatformPayout`);ok(res,r[0]);}catch(e){next(e);}});
app.get('/api/admin/users',auth,guard('ROLE_ADMIN'),async(req,res,next)=>{try{const [r]=await pool.query('SELECT * FROM users ORDER BY created_at DESC');ok(res,r.map(x=>({id:x.id,email:x.email,fullName:x.full_name,phone:x.phone,role:x.role,active:!!x.is_active,suspended:!!x.is_suspended,createdAt:x.created_at})));}catch(e){next(e);}});async function adminProfiles(req,res,next,t){try{const [r]=await pool.query(`SELECT u.*,p.* FROM users u JOIN ${t==='student'?'student_profiles':'owner_profiles'} p ON p.user_id=u.id`);ok(res,r.map(profile));}catch(e){next(e);}}app.get('/api/admin/students',auth,guard('ROLE_ADMIN'),(q,s,n)=>adminProfiles(q,s,n,'student'));app.get('/api/admin/owners',auth,guard('ROLE_ADMIN'),(q,s,n)=>adminProfiles(q,s,n,'owner'));
app.put('/api/admin/owners/:id/verify',auth,guard('ROLE_ADMIN'),async(req,res,next)=>{try{const v=req.query.verified!=='false';await pool.query('UPDATE owner_profiles SET verification_status=?,verified_at=? WHERE id=?',[v?'VERIFIED':'PENDING_VERIFICATION',v?new Date():null,req.params.id]);const [r]=await pool.query('SELECT u.*,p.* FROM users u JOIN owner_profiles p ON p.user_id=u.id WHERE p.id=?',[req.params.id]);ok(res,profile(r[0]),v?'Owner verified successfully!':'Owner verification reverted to pending.');}catch(e){next(e);}});app.put('/api/admin/users/:id/suspend',auth,guard('ROLE_ADMIN'),async(req,res,next)=>{try{const v=req.query.suspended==='true';await pool.query('UPDATE users SET is_suspended=? WHERE id=? AND role<>"ROLE_ADMIN"',[v,req.params.id]);const [r]=await pool.query('SELECT * FROM users WHERE id=?',[req.params.id]);ok(res,r[0],v?'User suspended successfully':'User reactivated successfully');}catch(e){next(e);}});app.get('/api/admin/jobs',auth,guard('ROLE_ADMIN'),async(req,res,next)=>{try{const [r]=await pool.query(`${jobSql} ORDER BY j.created_at DESC`);ok(res,r.map(x=>job(x,true)));}catch(e){next(e);}});app.delete('/api/admin/jobs/:id',auth,guard('ROLE_ADMIN'),async(req,res,next)=>{try{await pool.query("UPDATE catering_jobs SET status='CANCELLED' WHERE id=?",[req.params.id]);ok(res,null,'Job deleted/cancelled by admin');}catch(e){next(e);}});
app.get('/api/notifications',auth,async(req,res,next)=>{try{const [r]=await pool.query('SELECT * FROM notifications WHERE recipient_id=? ORDER BY created_at DESC',[req.user.id]);ok(res,r.map(x=>({...x,isRead:!!x.is_read})));}catch(e){next(e);}});app.get('/api/notifications/unread-count',auth,async(req,res,next)=>{try{const [r]=await pool.query('SELECT COUNT(*) count FROM notifications WHERE recipient_id=? AND is_read=FALSE',[req.user.id]);ok(res,r[0].count);}catch(e){next(e);}});app.put('/api/notifications/:id/read',auth,async(req,res,next)=>{try{await pool.query('UPDATE notifications SET is_read=TRUE WHERE id=? AND recipient_id=?',[req.params.id,req.user.id]);ok(res,null,'Notification marked as read');}catch(e){next(e);}});app.put('/api/notifications/read-all',auth,async(req,res,next)=>{try{await pool.query('UPDATE notifications SET is_read=TRUE WHERE recipient_id=?',[req.user.id]);ok(res,null,'All notifications marked as read');}catch(e){next(e);}});
app.post('/api/reports',auth,async(req,res,next)=>{try{const b=req.body;const result=await transaction(async c=>{const [[target]]=await c.query('SELECT ou.id owner_user_id,j.title FROM job_applications a JOIN student_profiles sp ON sp.id=a.student_id JOIN catering_jobs j ON j.id=a.job_id JOIN owner_profiles op ON op.id=j.owner_id JOIN users ou ON ou.id=op.user_id WHERE a.id=? AND sp.user_id=?',[b.applicationId,req.user.id]);if(!target)throw Object.assign(new Error('Application not found or not owned by student'),{status:404});const [created]=await c.query('INSERT INTO reports (reporter_id,target_user_id,job_id,application_id,report_type,description,expected_amount,received_amount,evidence_notes) VALUES (?,?,?,?,?,?,?,?,?)',[req.user.id,target.owner_user_id,b.jobId||null,b.applicationId||null,b.reportType,b.description,b.expectedAmount||null,b.receivedAmount||null,b.evidenceNotes||null]);await c.query("INSERT INTO notifications (recipient_id,title,message,type,related_entity_id) VALUES (?,?,?,?,?)",[target.owner_user_id,'Student Complaint Received',`A student submitted a complaint about \"${target.title}\". Please review it and cooperate with platform administrators.`,'REPORT_SUBMITTED',created.insertId]);return created.insertId;});ok(res,{id:result},'Report submitted to the owner and platform administrators.');}catch(e){next(e);}});app.get('/api/reports/my-reports',auth,async(req,res,next)=>{try{const [r]=await pool.query('SELECT * FROM reports WHERE reporter_id=?',[req.user.id]);ok(res,r);}catch(e){next(e);}});app.get('/api/admin/reports',auth,guard('ROLE_ADMIN'),async(req,res,next)=>{try{const [r]=await pool.query('SELECT r.*,u.full_name reporter_name,u.email reporter_email FROM reports r JOIN users u ON u.id=r.reporter_id ORDER BY r.created_at DESC');ok(res,r);}catch(e){next(e);}});app.put('/api/admin/reports/:id/resolve',auth,guard('ROLE_ADMIN'),async(req,res,next)=>{try{await pool.query('UPDATE reports SET status=?,admin_remarks=?,resolved_at=NOW() WHERE id=?',[req.body.status,req.body.adminRemarks||null,req.params.id]);const [r]=await pool.query('SELECT * FROM reports WHERE id=?',[req.params.id]);ok(res,r[0],'Report resolved successfully');}catch(e){next(e);}});
app.delete('/api/reports/:id',auth,async(req,res,next)=>{try{const result=await transaction(async c=>{const [[report]]=await c.query('SELECT id,target_user_id FROM reports WHERE id=? AND reporter_id=? FOR UPDATE',[req.params.id,req.user.id]);if(!report)throw Object.assign(new Error('Complaint not found'),{status:404});await c.query('DELETE FROM reports WHERE id=? AND reporter_id=?',[req.params.id,req.user.id]);await c.query("INSERT INTO notifications (recipient_id,title,message,type,related_entity_id) VALUES (?,?,?,?,?)",[report.target_user_id,'Complaint Withdrawn','The student withdrew the complaint. It has been removed from your complaints list.','REPORT_WITHDRAWN',report.id]);return report.id;});ok(res,{id:result},'Complaint withdrawn successfully.');}catch(e){next(e);}});app.use(express.static(staticDir));app.get('*',(req,res)=>res.sendFile(path.join(staticDir,'index.html')));app.use((e,req,res,next)=>{console.error(e);fail(res,e.status||500,e.status?e.message:'Internal server error');});
if(require.main===module)initializeDatabase().then(()=>app.listen(PORT,()=>console.log(`PartTime Job Platform listening on http://localhost:${PORT}`))).catch(e=>{console.error('Database initialization failed:',e.message);process.exitCode=1;});
module.exports=app;